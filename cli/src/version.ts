/**
 * The published version, as a constant rather than a read of `package.json`:
 * this package touches no files. A test asserts the two still agree, so a
 * release cannot go out identifying itself as the one before it.
 */
export const VERSION = '0.1.0'
